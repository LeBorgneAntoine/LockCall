<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TP3" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <item name="colorPrimary">@color/primary_color</item>

        <item name="inputBackgroundColor">@color/input_background_color_light</item>
        <item name="buttonBackgroundColor">@color/button_background_color_light</item>
        <item name="borderColor">@color/border_color_light</item>
        <item name="deepBackgroundColor">@color/deep_background_color_light</item>
        <item name="backgroundColor">@color/background_color_light</item>
        <item name="backgroundGlobal">@color/backgroundGlobal_light</item>
        <item name="colorPrimaryDark">@color/primary_color</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>